const assert = require('assert');
const kodiPrBot = require('../index.js');

describe('kodiPrBot', function () {
  it('has a test', function () {
    assert(false, 'kodiPrBot should have a test');
  });
});
